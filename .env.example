# Xiaomi Account Configuration
# Get your device ID by running: micli list (after setting MI_USER and MI_PASS)
MI_USER=your_xiaomi_account@example.com
MI_PASS=your_xiaomi_password
MI_DID=your_device_id_or_name

# Server Configuration
# SERVER_HOST: 监听地址，0.0.0.0 表示接受所有网络接口的连接
SERVER_HOST=0.0.0.0
SERVER_PORT=9527

# Static File Server (for audio playback)
# IMPORTANT: STATIC_SERVER_HOST 必须是小米音箱能访问的 IP 地址
# - 如果在树莓派上运行，填写树莓派的局域网 IP（如 192.168.1.100）
# - 不要使用 localhost 或 127.0.0.1，音箱无法访问
# - 可以通过 hostname -I 或 ip addr 查看树莓派 IP
STATIC_SERVER_HOST=192.168.1.100  # 修改为你的树莓派 IP
STATIC_SERVER_PORT=1810 # Xiaomi stock code

# Piper TTS Configuration (本地 TTS，无需网络)
# 语音模型会自动下载到 ~/.local/share/piper-voices/
PIPER_VOICE_ZH=zh_CN-huayan-medium  # 中文女声
PIPER_VOICE_EN=en_US-lessac-medium  # 英文女声
PIPER_SPEAKER=0                      # 说话人 ID（多说话人模型）
PIPER_LENGTH_SCALE=1.0               # 语速（1.0=正常，<1.0=快，>1.0=慢）

# Audio Cache Directory
AUDIO_CACHE_DIR=audio_cache

# Cloudflare Tunnel Token (Optional, if using docker-compose for tunnel)
# Get this from Cloudflare Zero Trust Dashboard
TUNNEL_TOKEN=

# Webhook Security (optional)
# Set this to verify GitHub webhook signatures
# GITHUB_WEBHOOK_SECRET=your_webhook_secret

# ============================================
# 必填配置 (Required Configuration)
# ============================================

# Xiaomi Account Configuration
# 获取设备 ID: micli list (需先设置 MI_USER 和 MI_PASS)
MI_USER=your_xiaomi_account@example.com
MI_PASS=your_xiaomi_password
MI_DID=your_device_id_or_name

# Static File Server (音频播放服务器)
# ⚠️ 重要: STATIC_SERVER_HOST 必须是小米音箱能访问的 IP 地址
# - 如果在树莓派上运行，填写树莓派的局域网 IP（如 192.168.1.100）
# - 不要使用 localhost 或 127.0.0.1，音箱无法访问
# - 可以通过 hostname -I 或 ip addr 查看树莓派 IP
STATIC_SERVER_HOST=192.168.1.100  # 修改为你的设备 IP
STATIC_SERVER_PORT=1810

# ============================================
# 服务器配置 (Server Configuration)
# ============================================

# Webhook Server
# SERVER_HOST: 监听地址，0.0.0.0 表示接受所有网络接口的连接
SERVER_HOST=0.0.0.0
SERVER_PORT=9527

# ============================================
# TTS 配置 (Text-to-Speech Configuration)
# ============================================

# Piper TTS (本地 TTS，无需网络)
# 语音模型会自动下载到 ~/.local/share/piper-voices/
# 💡 提示: 如果不配置 PIPER_VOICE_ZH，中文通知将使用音箱自带的 TTS（无需下载模型）
# PIPER_VOICE_ZH=zh_CN-huayan-medium  # 中文女声（可选，留空则使用音箱自带TTS）
PIPER_VOICE_EN=en_US-hfc_male-medium  # 英文男声 https://rhasspy.github.io/piper-samples
PIPER_SPEAKER=0                       # 说话人 ID（多说话人模型）
PIPER_LENGTH_SCALE=1.0                # 语速（1.0=正常，<1.0=快，>1.0=慢）

# Audio Cache Directory
AUDIO_CACHE_DIR=audio_cache

# ============================================
# 安全配置 (Security Configuration - Optional)
# ============================================

# GitHub Webhook Security
# 用于验证 GitHub webhook 签名
# GITHUB_WEBHOOK_SECRET=your_webhook_secret

# API Security
# 用于保护自定义 webhook 端点
# 生成强随机密钥: python -c "import secrets; print(secrets.token_urlsafe(32))"
# API_SECRET=your_strong_random_api_key

# ============================================
# 高级配置 (Advanced Configuration - Optional)
# ============================================

# Cloudflare Tunnel Token
# 如果使用 docker-compose 配置 Cloudflare Tunnel
# 从 Cloudflare Zero Trust Dashboard 获取
# TUNNEL_TOKEN=
